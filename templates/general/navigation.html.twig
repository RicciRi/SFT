<div class="sticky-wrap">
    <div class="navigation-menu">
        {% if app.user %}
            <a class="nav-link" href="{{ path('app_home') }}">Home</a>
            <a class="nav-link" href="{{ path('app_send') }}">Send</a>
            <a class="nav-link" href="{{ path('app_send_transfers') }}">Transfers</a>
            <a class="nav-link" href="{{ path('app_settings_general') }}">Settings</a>

            {% if app.user.isMainAccount or is_granted('ROLE_COMPANY_ADMIN') %}
                    <a class="nav-link" href="{{ path('app_employee') }}">Employee</a>
                {% if licenseType == freeType %}
                    <a class="nav-link" href="{{ path('app_price') }}">Price</a>
                {% endif %}
            {% endif %}

        {% else %}
            <a class="nav-link" href="{{ path('app_home') }}">Home</a>
            <a class="nav-link" href="{{ path('app_price') }}">Price</a>
            {#            <a class="nav-link" href="{{ path('app_home') }}">Solutions</a> #}
            <a class="nav-link navigation-button" href="{{ path('app_home') }}">Talk to an Expert</a>
        {% endif %}

    </div>
</div>